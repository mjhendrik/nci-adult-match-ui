# Base image 
FROM httpd:2.4
MAINTAINER jeremy.pumphrey@nih.gov

ENV INSTALL_PATH /usr/local/apache2/match

RUN apt-get update && apt-get install -y \
    && rm -rf /var/lib/apt/lists/* \
    && mkdir -p $INSTALL_PATH \
    && chmod 777 $INSTALL_PATH

WORKDIR $INSTALL_PATH

COPY ./dist/dev /usr/local/apache2/match

# This is a debug list
# RUN ls -alth /usr/local/apache2/match

CMD httpd-foreground -C 'DocumentRoot /usr/local/apache2/match/'
